# Copy this to .env.local and fill in real values.
# Variables prefixed EXPO_PUBLIC_ are included in the JS bundle — do NOT put secrets here.

# Base URL for the GymMaster backend API (include trailing slash)
EXPO_PUBLIC_API_URL=http://localhost:8087/
